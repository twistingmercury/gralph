# Gralph Concept

Gralph is to be port of the shell script [ralph.sh](./ralph.sh) written in Go.

It should do everything that the script does.

It will be installed in to the GOBIN dir so that is can be used anywhere.
